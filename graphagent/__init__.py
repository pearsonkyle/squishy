"""graphagent: knowledge-graph-augmented coding agent with an A/B harness."""

# graphagent ships inside the squishy distribution, so `graphagent --version`
# reporting its own separate number was just confusing. Keep this in step with
# `version` in pyproject.toml; they bump together.
__version__ = "0.2.0"
